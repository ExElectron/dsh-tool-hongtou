## 改动说明

本次 PR 为 `hongtou` 公文插件**内置爱弥斯联合印章**（`assets/seal-default.png`），作为默认公章覆盖层。用户无需任何配置即可获得加盖效果；配置 `seal: false` 或 `seal: "off"` 可跳过盖章，恢复干净版面。

相较于此前 merged 的 #1，本次核心变化：从「不内置任何印章资产」改为「内置爱弥斯联合印章作为默认」。印章图片为 1696×1696 透明底 PNG，通过 `<w:binData>` + VML 浮动图嵌入，`z-index:-1` 置于文字层之下，保证日期署名不被遮挡。

## Changes

This PR ships a **built-in Aemis Union seal** (`assets/seal-default.png`) as the default official-seal overlay for the `hongtou` document plugin. No configuration needed — documents are stamped by default. Set `seal: false` or `seal: "off"` to skip stamping.

Compared to the previously merged #1, the key change is: from "no seal images shipped" to "Aemis Union seal shipped as default." The seal is a 1696×1696 transparent PNG, embedded via `<w:binData>` + VML floating shape with `z-index:-1` behind the text layer.

## 変更点

本 PR は `hongtou` 公文書プラグインに**内蔵エイミス連合公印**（`assets/seal-default.png`）をデフォルトの公印オーバーレイとして同梱します。設定不要でデフォルト押印されます。`seal: false` または `seal: "off"` で押印をスキップ可能です。

以前マージされた #1 との主な違い：「公印画像を一切同梱しない」から「エイミス連合公印をデフォルトとして同梱」へ変更。公印は 1696×1696 の透過 PNG で、`<w:binData>` + VML フローティングシェイプにより `z-index:-1` でテキスト層の背後に配置されます。