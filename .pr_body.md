## 📌 功能概述（中文 / English / 日本語 / 한국어）

本次改动为 `hongtou` 公文插件新增**可选公章覆盖层**能力：盖章由配置驱动，默认不盖。**不内置任何印章资产**，仅提供可选的覆盖层框架——用户通过 `seal` 配置项自行指定公章图片路径，即可在「发文机关署名 + 成文日期」处加盖；未配置时输出与原版完全一致，零侵入、完全向后兼容。

技术实现采用 Word 2003 XML 的标准图片嵌入方式：图片二进制以 `<w:binData>` 存放于与 `<v:shape>` 同处的 `<w:pict>` 内，浮动定位锚定在成文日期段之后；`z-index:-1` 使正文文字压住红印，模拟真实印泥「黑字透红」的效果，日期与署名无论何时都不会被印章遮挡。公章随落款流动锚定，文章长短、是否翻页均不影响盖章位置。几何参数（直径、水平/垂直偏移、旋转角）可通过 `sealGeometry` 覆盖，方便不同机关调整章的大小与位置。

使用方式：配置 `seal` 为本地公章图片路径即启用盖章；配置为 `off` 或不配置则跳过盖章，保留干净版面。整个功能对既有流程零打扰，旧文档、旧模板、旧脚本全部原样可用。

测试覆盖：无章时输出不变（向后兼容）、有章时 binData 与 shape 结构正确、几何参数可覆盖。新增 3 个用例，原有 24 个用例全部通过。

### 一则痛心疾首的声明

本提交**不内置任何公章图片**。作者深知，一枚公章哪怕只是玩梗，亦可能被人误读、误用，乃至在分发过程中引发不必要的合规风险。作者纵然对亲手打磨的仿制公章有着难以割舍的感情，但为做一名遵纪守法、不给社区添麻烦的好公民，痛下决心将所有章图从提交中删除，仅保留可选覆盖层的功能框架。望各位理解。

---

## English

This change adds an **optional official-seal overlay** to the `hongtou` document plugin. Stamping is config-driven and off by default. **No seal images are shipped** — only the overlay framework is provided. Users specify a path to their own seal image via the `seal` config; when no image is configured, stamping is skipped and output is byte-for-byte identical to the original — zero intrusion, fully backward compatible.

The seal is embedded using the standard Word 2003 XML technique: the binary image is stored as `<w:binData>` inside the same `<w:pict>` as the `<v:shape>`, float-positioned after the date paragraph, with `z-index:-1` so the body text presses over the red ink. The seal is anchored to the flowing signature block and geometry can be overridden via `sealGeometry`.

Tests: no-seal output unchanged, with-seal binData/shape verified, geometry overridable. 3 new cases; 24 original all pass.

### A Grieved Statement

**No seal images are shipped in this PR.** The author knows that even a meme seal can be misread, misused, or create unnecessary compliance risks in distribution. Though deeply attached to the hand-crafted seal, the author has made the painful decision to remove all seal images and keep only the optional overlay framework — to be a law-abiding citizen who brings no trouble to the community.

---

## 日本語

本変更は `hongtou` 公文書プラグインに**任意の公印オーバーレイ機能**を追加します。押印は設定駆動で、**公印画像は一切同梱しません**。利用者は `seal` 設定で自分の公印画像パスを指定して有効化し、未設定の場合は押印をスキップします。

技術的には Word 2003 XML 標準の画像埋め込み方式を採用。`z-index:-1` により本文文字が赤い印影を覆い、日付と署名は隠れません。ジオメトリは `sealGeometry` で上書き可能。

### 悲痛な声明

**本PRには公印画像を一切含みません。** 遊び心の公印であっても、誤読・誤用・配布時のコンプライアンスリスクを招きかねないと作者は痛感し、手作りの公印を削除する決断をしました。コミュニティに迷惑をかけない遵法市民として、オーバーレイの枠組みのみを提供します。

---

## 한국어

이번 변경은 `hongtou` 문서 플러그인에 **선택적 직인 오버레이 기능**을 추가합니다. 날인은 설정 기반이며, **직인 이미지는 전혀 포함되지 않습니다.** 사용자는 `seal` 설정으로 자신의 직인 이미지 경로를 지정하며, 미설정 시 날인을 건너뜁니다.

기술적으로 Word 2003 XML 표준 이미지 임베딩 방식을 사용하며, `z-index:-1`로 본문 텍스트가 붉은 인영을 덮습니다. 지오메트리는 `sealGeometry`로 조정 가능합니다.

### 애통한 성명

**본 PR에는 직인 이미지가 전혀 포함되어 있지 않습니다.** 장난삼아 만든 직인이라도 오독·오용·배포 시 컴플라이언스 위험을 초래할 수 있음을 깨닫고, 저자는 수제 직인을 삭제하는 고통스러운 결정을 내렸습니다. 커뮤니티에 폐를 끼치지 않는 준법 시민으로서, 오버레이 프레임워크만을 제공합니다.
